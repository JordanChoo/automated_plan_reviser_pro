name: simple-workflow
description: A simple test workflow

documents:
  readme: README.md
  spec: SPECIFICATION.md

api:
  model: "gpt-5.5"
  reasoning_effort: high

rounds:
  output_dir: .apr/rounds/simple
